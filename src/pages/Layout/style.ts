import styled from "styled-components"
import { cores } from "../../style/GlobalStyle.ts"

export const Main = styled.main`
    min-height: calc(100vh-9,375rem);
    background-color: ${cores.tercearia}; 
`